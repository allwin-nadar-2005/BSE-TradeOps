# Supabase — use the ANON/PUBLIC key here (safe for the browser; RLS restricts to read-only).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Backend base URL (the combined Express server)
VITE_API_BASE_URL=http://localhost:4000
